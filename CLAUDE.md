# CLAUDE.md

The harness for this workspace lives in `AGENTS.md`, so that Claude Code, opencode, and any other
AGENTS.md-aware tool read exactly the same contract. It is imported below rather than duplicated —
two copies of a harness drift, and a drifted harness is worse than none.

@AGENTS.md

Project-specific stack, conventions, and commands are declared in `openspec/project.md`. Read it
before prescribing, implementing, or reviewing anything technical.
